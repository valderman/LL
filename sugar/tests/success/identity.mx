i : 1, o : ~1 |- cut
{ id : forall a . a -o a ->
    let id1' = id @ 1
    in connect id1' via
        { a -> a <-> i
        ; b -> b <-> o
        }
; mkid ->
    let a @ id' = mkid
    in let u, v = id'
    in v <-> u
}
