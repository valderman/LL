i : 1, o : ~1 |- connect
{ id : forall a . a -o a ->
    let id1' = id @ 1
    in connect via id1'
        { a -> a <-> i
        ; b -> b <-> o
        }
; mkid ->
    let a @ id' = mkid
    in let u, v = id'
    in v <-> u
}
