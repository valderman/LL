f : ~(forall a . forall b . forall c . ((a * b) * c) -o (a * (b * c))) |-
let a @ f = f in
let b @ f = f in
let c @ f = f in
let i , o = f in
let ab , c = i in
let a , b = ab in
connect o via
    { a' -> a <-> a'
    ; bc' -> connect bc' via
        { b' -> b' <-> b
        ; c' -> c' <-> c
        }
    }



